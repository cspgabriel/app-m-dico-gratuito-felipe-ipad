# Deploy feito pela Vercel via integração Git — este workflow está desabilitado
# O Vercel detecta pushes no main automaticamente via https://www.clinicafy.com.br
name: Deploy to GitHub Pages (DESABILITADO)

on:
  workflow_dispatch:  # somente manual — não roda em push

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy gerenciado pela Vercel. Ver https://www.clinicafy.com.br"
